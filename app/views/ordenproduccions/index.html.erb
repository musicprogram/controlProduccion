
<body class="fonpage">
  <h1 class="center">ORDENES DE PRODUCCIÓN</h1> 
  <br>
<h4 class="derecho"><span class="badge option animated wobble"> <%= @ordenproduccions.count %> </span></h4 >

  <%= form_tag ordenproduccions_path, :method => 'get' do %>
  <% content_tag :label do %>

  <%= text_field_tag :search, params[:search], class:'form-control'  %> <br>
  <%= submit_tag "Buscar por Referencia", :referencia => nil, class:'btn btn-succes'%>
  <% end %>
  <% end %>

  <div class="table-responsive ">
    <div class="tbla">
      <table class="table table-condensed table-hover tbla">
        <thead>
          <tr class="active tablasom">
            <th>Numero Orden</th>
            <th>Referencia</th>
            <th>Fecha</th>

            <th>Descripcion</th>


            <th>Cantidad</th>

            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @ordenproduccions.each do |ordenproduccion| %>
          <tr>
            <td>000<%= ordenproduccion.id %></td>
            <td><%= ordenproduccion.fechaprogramacion %></td>

            <td><%= ordenproduccion.referencia %></td>
            <td><%= ordenproduccion.descripcion %></td>


            <td><%= ordenproduccion.cantidad rescue nil %></td>

            <td><%= link_to "<i class='fa fa-scissors fa-3x encima'>".html_safe, ordenproduccion_descripcioncortes_path(ordenproduccion) %></td>
            <td><%= link_to "<i class='fa fa-eye fa-3x encima'>".html_safe, ordenproduccion %></td>
            <td><%= link_to "<i class='fa fa-pencil fa-3x encima'>".html_safe, edit_ordenproduccion_path(ordenproduccion) %></td>
            <td><%= link_to "<i class='fa fa-eraser fa-3x encima'>".html_safe, ordenproduccion, method: :delete, data: { confirm: 'Estas seguro?' } %></td>



          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <%= link_to "<i class='fa fa-plus-circle fa-4x'></i>".html_safe, new_ordenproduccion_path %>

  <%= will_paginate @ordenproduccions, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente &raquo;' %>
  <br>

</body>