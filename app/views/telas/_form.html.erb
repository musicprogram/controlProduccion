<%= form_for(@tela) do |f| %>
  <% if @tela.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tela.errors.count, "error") %> prohibited this tela from being saved:</h2>

      <ul>
      <% @tela.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :telanombre %><br>
    <%= f.text_field :telanombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
